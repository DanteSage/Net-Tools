# Net Tools 更新日志

## v1.1.3 (2026-06-01)

### ✨ 新增功能

#### 可视化 SFTP 文件管理器
- **功能特性**：在 SSH 远程终端中无缝扩展出右侧可视化 SFTP 文件面板，一键轻松管理远程服务器文件。
- **核心能力**：
  - **目录导航**：支持双击文件夹深度导航及上方高精面包屑层级跳转。
  - **安全传输**：复用既有 SSH 物理 Socket 会话，提供高安全性极速文件上传与下载。
  - **右键操作**：文件列表中支持右键进行“下载”、“重命名”、“删除”及“新建文件夹”操作。
  - **拖拽上传 (Drag & Drop)**：支持从本地 Windows 资源管理器直接拖拽文件至远程目录列表中自动触发上传。
  - **进度跟踪**：底部集成磨砂进度指示条，直观反映文件传输进度与百分比。

#### 可视化 FTP 协议管理集成
- **原生零依赖驱动**：采用纯原生 TCP Socket 实现轻量级 FTP 引擎，不引入任何外部第三方库，对可执行包大小及性能 **零负面影响**。
- **智能降级备用通道**：当设备的 SSH 会话不支持 SFTP 协议时，系统能够静默进行备用 FTP 连接侦测并平滑无感降级切换为 FTP 通道。
- **Telnet 伴生文件管理器**：为 Telnet 交互终端在后台静默建立 21 端口的伴生 FTP 复用通道，完美复用设备凭证，生命周期自动销毁托管。
- **高并发端口延迟保护**：针对网络交换机（Huawei/H3C）二阶段数据通道分配延时，提供 800ms 握手防并发缓冲及 500ms 首次目录载入保护，彻底解决 ECONNREFUSED 错误。
- **显式独立双核管理**：在 SSH 会话工具栏提供显式的 SFTP 和 FTP 独立选项，赋予运维工程师 100% 灵活性。

#### 磨砂玻璃右侧滑出式「在线文本编辑器」
- **高颜半透抽屉布局**：基于 HSL 现代半透明磨砂玻璃设计，右侧滑出平滑过渡拉出（占 60% 屏幕宽度），允许左侧命令行和右侧编辑器完美同屏并重绘终端自适应。
- **高级快捷输入辅助**：原生完美支持 Tab 键键入自动转换为 4 空格缩进，以及回车（Enter）自动提取前导空格以继承前导对齐缩进。
- **绝对滚动行号对齐**：高性能生成纯文本行号指示器，严格控制滚动高度监听，使得行号与文本编辑区纵向高度绝对同步。
- **Ctrl+S 远程秒级秒写**：支持通过 `Ctrl+S` 全局快捷键或保存按钮，基于原有 SFTP/FTP 物理会话秒级将内容安全覆写回远端，并自动热重载文件列表。
- **智能未保存更改阻断**：退出/关闭编辑器时会智能对比原始内容并弹窗询问是否丢弃，强力防范运维编辑丢失。

---

## v1.1.2 (2026-01-04)

### 🐛 Bug 修复

- 修复 Ruijie 设备概率存在无法进入 enable 模式问题
- 修复 SSH、Telnet 对象重复定义问题

---

## v1.1.1 (2025-12-30)

### 🐛 Bug 修复

#### 华为设备批量执行失败问题
- **问题描述**：华为设备批量执行命令时报 `read ECONNRESET` 错误，导致任务显示失败
- **原因分析**：华为设备在收到 `quit` 命令后会主动关闭 Telnet 连接，这个正常的关闭行为被程序误判为连接错误
- **解决方案**：添加退出命令标记，当已发送 `quit` 命令后收到 `ECONNRESET`，视为正常关闭而非错误

#### SSH 连接华为设备报 "Unknown DH group" 错误
- **问题描述**：通过 SSH 连接部分华为设备时，提示密钥交换算法不支持
- **解决方案**：在 SSH 测试连接和正式连接中添加更多 DH 组支持：
  - `diffie-hellman-group-exchange-sha1`
  - 完整的 `serverHostKey` 配置
  - 更多 HMAC 算法支持

#### 锐捷设备 SSH 连接后无法进入命令行
- **问题描述**：锐捷设备通过 SSH 连接时显示“连接成功”但终端不显示命令行提示符
- **原因分析**：锐捷设备 SSH 登录后直接进入特权模式（提示符以 `#` 结尾），但 autoEnable 逻辑期望先看到用户模式提示符（以 `>` 结尾），导致状态停留在 waiting，数据被拦截不显示
- **解决方案**：在 autoEnable 的 waiting 状态中添加特权模式检测，如果设备已在特权模式（`#`），直接跳过 enable 流程并正常显示数据

### ✨ 功能增强

#### 设备导入功能优化
- **自动检测分隔符**：支持逗号(,)、分号(;)、制表符(TAB)、竖线(|) 作为分隔符
- **表头别名支持**：支持更多中英文表头名称：

| 标准字段 | 支持的别名 |
|---------|-----------|
| name | 设备名称、设备名、名称、主机名、hostname、device |
| host | IP地址/主机名、IP、IP地址、地址、address |
| type | 设备类型、类型、厂商、vendor |
| protocol | 协议、连接协议、连接方式 |
| username | 用户名、用户、账号、user、account |
| password | 密码、pass、pwd |
| group | 分组、组、类别、category |
| description | 描述、备注、remark、note、comment |

- **调试日志**：按 F12 打开开发者工具可查看导入解析过程
- **编码检测**：支持 UTF-8/UTF-16/GBK 编码的文件读取

### 🔧 优化调整

- 统一所有设备禁用分页命令后的等待时间为 1000ms
- 添加批量执行调试日志，便于排查问题
- 退出命令后等待时间从 500ms 增加到 1000ms，确保设备正常处理

---

## v1.1.0 (2025-12-29)

### 初始功能
- 设备管理（SSH/Telnet/串口）
- 远程终端
- 批量命令执行
- 命令模板
- 配置备份
- 网络工具（Ping、端口扫描、路由追踪等）
